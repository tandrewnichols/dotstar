import React from 'react';

export interface Props {
}

export default function ${1:`!v join(split(g:Abolish.titlecase(expand('%:t:r')), ' '), '')`} (${2} : Props) : React.ReactElement {
  return (
    ${3}
  );
}
