" Language:    JavaScript

" Syntax highlighting for describe and context
syn keyword Constant angular $scope $rootScope $location $anchorScroll $animate $animateCss $cacheFactory $templateCache $compile $controller $document $exceptionHandler $filter $httpParamSerializer $httpParamSerializerJQLike $http $xhrFactory $httpBackend $interpolate $interval $locale $log $parse $q  $rootElement $sceDelegate $sce $templateRequest $timeout $window $injector
syn keyword Structure require
