import React from 'react';
import { shallow } from 'enzyme';
import `!v g:Abolish.mixedcase(expand('%:t:r:r'))` from './`!v expand('%:t:r:r')`';

describe('`!v join(split(expand('%:.:r:r'), 'src/')[1:], '/')`', () => {
  ${0}
});
